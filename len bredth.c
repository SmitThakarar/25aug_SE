#include<stdio.h>
main()
{
	int length,bredth;
	printf("enter the length:");
	scanf("%d",&length);
	printf("enter the bredth");
	scanf("%d",&bredth);
	printf("\n area of rectangle %d",length*bredth);
	
}
